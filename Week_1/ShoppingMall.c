#include <stdio.h>

int main() {
  int amount;
  scanf("%d", &amount);

  int receive[100];

  for (int i = 0; i < amount; i++) {
    scanf("%d", &receive[i]);
  }

  int sell[100];

  for (int i = 0; i < amount; i++) {
    scanf("%d", &sell[i]);
  }

  int stock[100];

  for (int i = 0; i < amount; i++) {
    stock[i] = receive[i] - sell[i];
  }

  int stock_id;
  scanf("%d", &stock_id);
  printf("%d\n", stock[stock_id - 1]);

  for (int i = 0; i < amount; i++) {
    printf("%d ", stock[i]);
  }

  return 0;
}

malloc 함수가 반환하는 것은 void형 포인터로, 우리가 사용하기 위해서는 원하는 자료형의 포인터로 형변환을 해줘야 한다.이렇게 형변환을 해줘야 하는 이유는 무엇인지 설명하 시오. (포인터 변수의 자료형이 왜 필요한가?)

:
`malloc` 함수가 반환하는 포인터는 `void*` 타입으로, 이는 특정 자료형을 가리키지 않는 일반적인 포인터입니다. 따라서, `malloc`이 반환한 포인터를 사용하려면 원하는 자료형의 포인터로 형변환을 해야 합니다. 이는 포인터가 가리키는 데이터의 자료형을 명시하여, 컴파일러가 해당 메모리 주소에 저장된 데이터를 올바르게 해석하고 처리할 수 있도록 하기 위함입니다. 자료형을 지정하지 않으면, 컴파일러는 메모리 접근 시 오류를 발생시킬 수 있습니다.

주어진 프로그램 실행에 대한 설명 중 맞는 것을 고르자면:
	•	a. argc에 들어가는 값은 4이다.: 맞습니다. `argc`는 인자의 개수를 나타내며, 프로그램 이름도 포함하므로 총 4개 (`test`, `123`, `abc`, `456`)가 됩니다.
	•	b. argv에 들어가 있는 값은 123이다.: 맞습니다. `argv` 배열은 프로그램 이름 다음의 첫 번째 인자인 `123`을 가리킵니다.
	•	d. argv는 문자열의 시작 주소값을 가지고 있는 배열이다.: 맞습니다. `argv`는 문자열(문자 배열)의 시작 주소를 가리키는 포인터들의 배열입니다.
**c. 123과 456은 정수이다.**는 틀렸습니다. 이 값들은 명령줄에서 문자열로 전달되며t, 프로그램 내에서 정수로 변환하지 않는 한 문자열로 취급됩니다.
